using UnityEngine;
using System.Collections;
using System;

public class EvolutionLogicTest : MonoBehaviour {

	// Use this for initialization
	void Start () {
	}
	
	// Update is called once per frame
	void Update () {
	}

    /*Test 1: data init 
        evolution data should not be initialized with 0. should be initialized with
        precalculated lastEvoMeter and evoAverageCum otherwise evoAverageCum will
        increase even when evoMeter stays the same
    */
}
